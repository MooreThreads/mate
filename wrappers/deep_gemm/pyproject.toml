[build-system]
requires = ["setuptools>=77", "packaging>=24", "mate>=0.1.1"]
build-backend = "setuptools.build_meta"

[project]
name = "mate-deep_gemm"
version = "0.1.2"
description = "A Python wrapper of MATE DeepGEMM"
requires-python = ">=3.10"
authors = [
  { name = "MATE Team" }
]
